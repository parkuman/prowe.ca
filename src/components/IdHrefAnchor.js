import React from "react";
import styled from "styled-components";

const IdHrefAnchor = styled.a`
  display: block;
  position: relative;
  top: -60px; // height of navbar
  visibility: hidden;
`;

export default IdHrefAnchor;
